# Big O

Use this folder for complexity notes, small measurement experiments, and
revisiting why an implementation is `O(n)`, `O(log n)`, `O(n log n)`, or worse.

Good note shape:

```txt
problem name
input size
time complexity
space complexity
why
tradeoffs
```
